аиболее основной путь для формирования лямбда выражения

(parameters) -> { body };

фигурная скобка требуется только для много строкового лямбда выражения

другая важная вещь также ка и обычный объект java - лямбда выражена всегда
имеют специальный тип.